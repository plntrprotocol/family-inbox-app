name: Deploy to Cloudflare Pages

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Wrangler
        run: npm install -g wrangler

      - name: Authenticate
        run: wrangler login --api-token ${{ secrets.CLOUDFLARE_API_TOKEN }}

      - name: Create D1 Database
        run: |
          wrangler d1 create family-inbox --json > d1_db.json
          echo "DB_ID=$(cat d1_db.json | jq -r '.uuid')" >> $GITHUB_ENV

      - name: Apply Schema  
        run: wrangler d1 execute ${{ env.DB_ID }} --file=schema.sql

      - name: Deploy to Cloudflare Pages
        uses: cloudflare/pages-action@v1
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          projectName: family-inbox
          directory: _static
